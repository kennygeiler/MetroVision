---
phase: 09-shot-boundary-fusion-policy-consensus-and-prune-auxiliary-detector-peaks
plan: "02"
type: plan
wave: 2
depends_on:
  - "01"
files_modified: []
autonomous: false
requirements: []
---

<objective>
Expose fusion policy in the **detect-only** CLI and record how to benchmark against **`eval/runs/STATUS.md`** targets; optionally add a small **offline sweep** helper that replays static `cutsSec` JSON (no video) for policy comparison.
</objective>

<task id="09-02-01">
<title>`detect-export-cuts` — `--fusion-policy` when merging extras</title>
<read_first>
- `scripts/detect-export-cuts.ts` (where interior cuts are produced; `--extra-cuts` path)
- `src/lib/boundary-fusion.ts` (after 09-01)
</read_first>
<action>
Extend **`scripts/detect-export-cuts.ts`**:

- New flag **`--fusion-policy merge_flat|auxiliary_near_primary|pairwise_min_sources`** (default **`merge_flat`**).
- When **`--extra-cuts`** (or env-based extras already merged in pipeline) produces **additional** interior candidates beyond the detector-only list, apply **`fuseBoundaryCutStreams(detectorInteriorCuts, extraCuts, policy)`** instead of only `mergeInteriorCutSec` / inline cluster — **or** clearly document that extras are fused via the selected policy after detector `cutsSec` is computed.

Ensure **default** behavior matches **today** when `merge_flat` (no metric change on Ran baseline without extras).

Update **`usage()`** stderr text with the new flag.
</action>
<acceptance_criteria>
- `rg "fusion-policy" scripts/detect-export-cuts.ts` matches.
- Without `--extra-cuts`, Ran-equivalent command produces **same** `cutsSec` length as before for same video/env (spot-check or unit test on mocked splits if needed).
- `pnpm test` exits 0.
</acceptance_criteria>
</task>

<task id="09-02-02">
<title>Docs + STATUS cross-link</title>
<read_first>
- `eval/runs/STATUS.md`
- `docs/pipeline-analysis.md` §3
- `docs/tuning-flow.md`
- `AGENTS.md` eval bullet
</read_first>
<action>
- **`docs/pipeline-analysis.md`:** Add bullet under evaluation workflow: fusion policies + link to **`boundary-fusion.ts`** and **`--fusion-policy`**.
- **`docs/tuning-flow.md`:** One row or bullet under **Adjust** / repo map for `--fusion-policy`.
- **`AGENTS.md`:** One clause on `detect-export-cuts` + `--fusion-policy`.
- **`eval/runs/STATUS.md`:** Under “Next roadmap slice”, confirm Phase 9 plans exist; add one line: benchmark improvement claims must cite **pred JSON path** + **fusion policy** + env snapshot.
</action>
<acceptance_criteria>
- `rg "fusion-policy" docs/pipeline-analysis.md docs/tuning-flow.md AGENTS.md` each has ≥1 match.
- `rg "boundary-fusion" docs/pipeline-analysis.md` matches.
</acceptance_criteria>
</task>

<task id="09-02-03">
<title>Optional: `scripts/eval-fusion-offline.ts` + npm script</title>
<read_first>
- `scripts/eval-pipeline.ts` (load gold/pred)
- `src/lib/boundary-fusion.ts`
</read_first>
<action>
Optional if timeboxed: **`pnpm eval:fusion-offline`** — args: **`--primary`** JSON path (or `cutsSec`), **`--auxiliary`** JSON path, **`--policy`**, **`--gold`** optional for instant `eval:pipeline` print. Skips video; for grid search on **static** TransNet exports vs saved PyScene `cutsSec`.

If skipped, note in **`09-VALIDATION.md`** “deferred” and rely on `detect-export-cuts` + `--extra-cuts` for integration proof.
</action>
<acceptance_criteria>
- If implemented: `rg "eval:fusion-offline" package.json` matches and `pnpm exec tsx scripts/eval-fusion-offline.ts` prints usage without args.
- If deferred: **`09-VALIDATION.md`** amended with “Offline fusion CLI deferred” and task marked N/A in execution notes.
</acceptance_criteria>
</task>

## must_haves

- Operators can name a **fusion policy** in run logs alongside merge gap and detector (see **`eval/runs/README.md`** checklist).
