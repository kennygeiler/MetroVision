---
phase: 07-shot-boundary-fn-analysis-list-gold-cuts-with-no-predicted-match-within-tolerance
plan: "02"
type: plan
wave: 1
depends_on:
  - "01"
files_modified: []
autonomous: false
requirements: []
---

<objective>
Let operators write FN/FP reports into `eval/runs/` with one command and cross-link the workflow in docs.
</objective>

<task id="07-02-01">
<title>Markdown + file output for `eval:boundary-misses`</title>
<read_first>
- `scripts/eval-boundary-misses.ts` (full file)
- `scripts/eval-boundary-deltas.ts` (how `--out` is handled, if present)
- `docs/tuning-flow.md` (repo map table)
</read_first>
<action>
Extend **`scripts/eval-boundary-misses.ts`** CLI:

1. **`--markdown`** — when set, write human-readable Markdown instead of plain text stdout (same sections: header paths, tolerance, P/R/F1 line, `## False negatives (gold)`, bullet list of seconds, `## False positives (pred)`).

2. **`--out PATH`** — when set, write the selected format (**JSON** if `--json`, else **Markdown** if `--markdown`, else **plain text**) to **`PATH`** using UTF-8. If `--out` is set, keep stderr summary optional: either remain silent on stdout or print one line `Wrote PATH` to stderr (pick one behavior and document in `usage()`).

3. Update **`usage()`** printed by the script to document `--markdown` and `--out`.

4. If both stdout and `--out` omitted for markdown/json modes, keep current stdout behavior for backward compatibility.
</action>
<acceptance_criteria>
- `rg "--markdown" scripts/eval-boundary-misses.ts` matches.
- `rg "--out" scripts/eval-boundary-misses.ts` matches.
- Running  
  `pnpm eval:boundary-misses -- eval/gold/smoke.json eval/predicted/smoke.json --tol 0.5 --markdown --out /tmp/mv-misses-test.md`  
  creates a file whose content includes the substring **`False negatives`** and **`F1`**.
- `pnpm test` still exits **0** (add a small unit test only if you refactor argv parsing into a testable pure function; otherwise skip new test file).
</acceptance_criteria>
</task>

<task id="07-02-02">
<title>Documentation cross-links</title>
<read_first>
- `docs/tuning-flow.md`
- `eval/runs/README.md`
- `AGENTS.md` (eval bullet)
</read_first>
<action>
- In **`docs/tuning-flow.md`** repo map row for FN/FP: add note that **`--markdown --out eval/runs/....md`** is the recommended log pattern.
- In **`eval/runs/README.md`**: add a short subsection **FN/FP report** with example command using `pnpm eval:boundary-misses` + `--markdown` + `--out`.
- In **`AGENTS.md`**: append to the eval sentence that **`--markdown` / `--out`** exist for run logs.
</action>
<acceptance_criteria>
- `rg "boundary-misses" docs/tuning-flow.md eval/runs/README.md AGENTS.md` each returns at least one match line.
- `eval/runs/README.md` contains the substring **`--markdown`**.
</acceptance_criteria>
</task>

## Verification criteria (plan)

- Manual: generated markdown opens in editor without broken encoding; counts match `eval:pipeline` for same inputs.

## must_haves

- Operators can append structured FN/FP artifacts under `eval/runs/` without copy-paste from terminal.
