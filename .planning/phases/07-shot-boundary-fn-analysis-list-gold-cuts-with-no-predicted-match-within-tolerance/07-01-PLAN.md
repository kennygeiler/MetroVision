---
phase: 07-shot-boundary-fn-analysis-list-gold-cuts-with-no-predicted-match-within-tolerance
plan: "01"
type: plan
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: []
---

<objective>
Deduplicate gold/predicted `cutsSec` JSON parsing into a shared module and add Vitest coverage so eval CLIs cannot drift on file shape.
</objective>

<task id="07-01-01">
<title>Add `extractCutsSecFromEvalJson` helper module</title>
<read_first>
- `scripts/eval-pipeline.ts` (lines with `extractCuts` / `loadJson`)
- `scripts/eval-boundary-misses.ts`
- `scripts/eval-boundary-deltas.ts`
- `scripts/detect-export-cuts.ts` (`extractGoldCuts`)
- `src/lib/boundary-eval.ts` (`normalizeCutList` behavior — do not duplicate normalization in loader unless intentional)
</read_first>
<action>
Create **`src/lib/eval-cut-json.ts`** exporting:

```ts
export function extractCutsSecFromEvalJson(data: unknown): number[];
```

**Semantics:** Match existing behavior everywhere today:

- If `data` is `number[]` (or array coerced with `Number`), keep only finite `>= 0` (same filters as `eval-pipeline.ts` `extractCuts`).
- If `data` is object with `cutsSec` array, same mapping.
- Otherwise throw `Error` whose `.message` contains the substring **`Expected a number[] or an object with cutsSec: number[]`**.

Do **not** call `normalizeCutList` inside this helper (callers / `evalBoundaryCuts` already normalize).

Replace inline `extractCuts` / `extractGoldCuts` implementations by importing this helper in:

- `scripts/eval-pipeline.ts`
- `scripts/eval-boundary-misses.ts`
- `scripts/eval-boundary-deltas.ts`
- `scripts/detect-export-cuts.ts` (replace `extractGoldCuts` body with call to helper; keep function name as thin alias if needed for readability)

Preserve **`detect-export-cuts.ts`** `loadJsonArrayCuts` unchanged (different shape: raw JSON array file for `--extra-cuts`).
</action>
<acceptance_criteria>
- `rg "extractCutsSecFromEvalJson" src/lib/eval-cut-json.ts` returns a match.
- `rg "function extractCuts\\(" scripts/eval-pipeline.ts` has **zero** matches (function removed or renamed after migration).
- `pnpm test` exits **0**.
- `pnpm eval:smoke` exits **0**.
- `pnpm eval:boundary-misses -- eval/gold/smoke.json eval/predicted/smoke.json --tol 0.5` exits **0** and stdout contains `F1=1.0000`.
</acceptance_criteria>
</task>

<task id="07-01-02">
<title>Unit tests for `eval-cut-json`</title>
<read_first>
- `src/lib/eval-cut-json.ts`
- `src/lib/__tests__/boundary-eval.test.ts` (Vitest style)
</read_first>
<action>
Add **`src/lib/__tests__/eval-cut-json.test.ts`** with cases:

1. `{ cutsSec: [1, 2.5, "3"] }` → `[1, 2.5, 3]` (numeric coercion consistent with pipeline).
2. Raw array `[10, NaN, -1, 20]` → `[10, 20]` (drop non-finite and negatives).
3. `{}` throws; message includes **`cutsSec`** or the shared expected substring above.
</action>
<acceptance_criteria>
- File `src/lib/__tests__/eval-cut-json.test.ts` exists.
- `pnpm test` exits **0** and output lists **`eval-cut-json.test.ts`** with at least **3** tests passed.
</acceptance_criteria>
</task>

## Verification criteria (plan)

- `pnpm test` and `pnpm eval:smoke` green after both tasks.
- No remaining duplicate `extractCuts` implementations in the four scripts (grep `extractCuts` under `scripts/` only matches comments or removed).

## must_haves (goal-backward)

- Single source of truth for eval JSON cut extraction used by boundary miss / delta / pipeline scripts.
- Automated tests prevent regression on accepted JSON shapes.
